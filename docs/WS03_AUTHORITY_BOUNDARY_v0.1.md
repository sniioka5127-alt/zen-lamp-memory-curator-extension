# WS-03 Authority Boundary v0.1

The global header is a navigation surface only.

Selecting a global or Tools-local destination does not approve ContextItems, persist memory, authorize transfer, execute a provider, adopt a Roundtable interpretation, finalize a Human Decision, or execute that decision. Navigation state and artifact authority state remain separate.
